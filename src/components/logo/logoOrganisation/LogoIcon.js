import logoIcon from 'assets/images/logo_icon.png';

// ==============================|| LOGO ICON SVG ||============================== //

const LogoIcon = () => {

  return <img src={logoIcon} alt={name} width="40" />;
};

export default LogoIcon;
